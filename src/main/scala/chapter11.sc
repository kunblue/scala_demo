//val √ = scala.math.sqrt _
//√(9)

Thread.`yield`()

1 to 10
1.to(10)

for (i <- 1 to 10) {
  print(i)
}


1351654646 toString

1351654646.toString()


class Fraction(n: Int, d: Int) {
  
}

object Fraction {
  def apply(n: Int, d: Int): Fraction = new Fraction(n, d)
}

val result = Fraction(2, 3) * Fraction(4, 5)